<div>
  <div class="row">
    <div class="col-md-12">
      <h1 class="green">ARTICLES</h1><br>
      <hr width="945">
    </div>
  </div>
  <div class="row">
    <% @articles.each do |article| %>
      <% if article.image_path? %>
        <div class="col-md-3">
          <%= image_tag(article.image_path, :width => 175, :height => 175) %>
        </div>
      <% end %>
      <div class="col-md-9">
        <h2><%= link_to article.title, article, class: 'article-links' %></h2>
        <small class="author">
          By <%= article.user.username %>
        </small><br><br>
        <p><%= article.body[0...400] %>...</p>
      </div>
      <div class="col-md-12">
        <hr width="945">
      </div>
    <% end %>
  </div>
  <h4 class="text-center paginate"><%= will_paginate @articles %></h4>
  <div class="row">
    <div class="text-center">
      <% if policy(Article.new).create? %>
        <%= link_to "New Article", new_article_path, class: 'devise btn btn-success' %>
      <% end %>
    </div>
  </div>
</div><br><br>